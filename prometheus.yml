global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'orders'
    metrics_path: '/metrics' # Default path for the OTel Prometheus exporter
    static_configs:
      - targets: ['ordersservice.application:8080'] # Use your service name and internal port